name: probe2

on:
  push:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    env:
      DSH_HOME: ${{ runner.temp }}/dsh-home
    steps:
      - run: echo "probe2 smoke ok $DSH_HOME"
